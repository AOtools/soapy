language: python
python:
  - "2.7"
  - "3.5"
addons:
  apt:
    packages:
    - python-numpy
    - python-scipy
    - python-pyfftw
    - libfftw3-dev
# command to install dependencies
install: "pip install astropy"
# command to run tests
script: nosetests
